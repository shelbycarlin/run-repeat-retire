import React from 'react';

const ShoeCard = () => {
  //need a fetch for get request for current rotations 
  //then with in we need to json the response
  //we need to dispatch to state the current shoe rotation 
  //we need a for loop to create the shoe cards and have them redered for the shoecards 

  return (
    <div className='ShoeCards'>
      <h2>ShoeCard</h2>
    </div>
  )
}
export default ShoeCard;